/**
 * List config files here (relative to moddir) to have them added to the exec config menu list
 * Left side is the filename, right side is the text to be added to the menu
 */
Configs
{
	"cfg/gamesettings/default/server_reset_soft.cfg"			"Server Reset Soft"
	"cfg/gamesettings/default/server_reset_hard.cfg"			"Server Reset Hard"
	"cfg/gamesettings/default/server_reset_full.cfg"			"Server Reset Full"
}
